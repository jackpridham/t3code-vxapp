# Duplicate Logic Rules — t3code-vxapp

Before adding a helper, search for an existing equivalent. If you find one
that's _almost_ right, extend it rather than fork it. If you find two
equivalents already, consolidate them as part of your current change.

A PR that adds a third copy of the same helper is rejected.
